```python
def username():
    name = input("username:")
    print(f"your name is {name}")

username()
```
```python
Iterate thru a list
thislist = ["apple", "banana", "cherry"]
for x in thislist:
  print(x)

thislist = ["apple", "banana", "cherry"]
i = 0
while i < len(thislist):
  print(thislist[i])
  i = i + 1
```
```python
and 	Returns True if both statements are true
or	Returns True if one of the statements is true
not	Reverse the result, returns False if the result is true
```

```python
name = Mohamed
print(name[1]) = o
print(name[-1]) = d
print(name[2:5]) = ham      # prints from 2 till 4(before)
name = ["mohamed", "osman", "Abdi"]
name.append - adds to a list
name.remove - removes fromm a list
name.pop - deletes last item on list and prints it 
name.insert[2] - adds to a list at 2
name.sort - sorts a string 
print("Mohamed" in name) = True # returns true if value is in list

Dict
name["name"] = "Sparta" #adds a new key to a dict
```